language: cpp

addons:
    apt:
        packages:
        - libpthread-stubs0-dev
        - libgl1-mesa-dev
        - libx11-dev
        - libx11-xcb-dev
        - libxcb-image0-dev
        - libxrandr-dev
        - libxcb-randr0-dev
        - libudev-dev
        - libfreetype6-dev
        - libglew-dev
        - libjpeg8-dev
        - libgpgme11-dev
        - libsndfile1-dev
        - libopenal-dev
        - libjpeg62
        - cmake

env:
    global:
        - DISPLAY=:99

before_install:
    - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
    - sudo apt-get update -qq

install:
    - sudo apt-get install -qq g++-8
    - sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-8 90
    - sudo apt-get install libsfml-dev
    - sudo apt-get install pulseaudio
    - sudo alsa force-reload
    - pavucontrol

before_script:
    - "/sbin/start-stop-daemon --start --quiet --pidfile /tmp/custom_xvfb_99.pid --make-pidfile --background --exec /usr/bin/Xvfb -- :99 -ac -screen 0 1280x1024x24"
    - sleep 3
      # Compiles but then I don't know how to link it properly in CMake
      #- git clone https://github.com/sfml/sfml 
      #- cd sfml 
      #- git checkout 2.5.0
      #- mkdir build && cd build
      #- cmake .. $CMAKE_FLAGS -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=../install
      #- cmake --build . --target install
      #- cd ../..

script:
    - mkdir build && cd build
    - cmake ..  $CMAKE_FLAGS -DBUILD_TESTS=1 -DOS_TRAVIS=1
    - make
    - ./chip_8_cpp_tests

matrix:
  include:

  - name: "Linux GCC Dynamic"
    os: linux
    dist: xenial
    compiler: gcc


